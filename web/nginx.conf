server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Never cache runtime config — it is environment-specific.
  location = /config.js {
    add_header Cache-Control "no-store";
  }

  # SPA: route everything else to index.html.
  location / {
    try_files $uri $uri/ /index.html;
  }
}

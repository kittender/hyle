{
  "name": "hyle-e2e-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end tests for the Hylé web UI (Playwright)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}

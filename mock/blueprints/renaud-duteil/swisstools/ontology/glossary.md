# Glossary

Keep this list tiny and universal — terms that apply no matter the language.

## Artifact
Anything the build produces: a binary, a bundle, a container image.

## Pipeline
The ordered set of automated steps from commit to deployable artifact.

## Convention
A decision recorded once so it never has to be re-argued.
